- Chạy project:
mở terminal phần backend, chạy file loadData rồi chạy server.js
mở terminal airline-website bên trong frontend, npm install rồi chạy npm start.
- Cấu trúc thư mục frontend: bên trong pages là các giao diện page,
- fetch API: 
ví dụ fetchAPI get promotion: "const response = await fetch('http://localhost:3000/api/users/promotions',".
-Còn thiếu chức năng: đặt vé, huỷ vé, tìm kiếm ở trang chủ.
-Các chức năng của tài khoản admin 
- sửa giao diện trang Promotion, 